package print

import (
// "fmt"
// "github.com/stretchr/testify/assert"
// "os"
// "testing"
)

func TestPrint(T *testing.T) {
	// assert := assert.New(T)

	// p := NewPrinter("printer1")
	// p.Print(os.Stdout)
	// p.Print(os.Stdout)
	// p.SetPrint(Printing)
	// p.Print(os.Stdout)
	// assert.Panics(p.Print(os.Stdout), "this code need to panic")
}
